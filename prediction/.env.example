# Carten Prediction Engine — Environment Variables
# Copy this file to .env and fill in your API keys.
# All keys are optional — the engine runs on historical patterns without them.

# ── INRIX Traffic (reuse from CartenDemo/server/.env) ─────
APP_ID=
HASH_TOKEN=
AUTH_TOKEN_URL=https://api.iq.inrix.com/auth/v1/appToken

# ── OpenWeatherMap (free tier: 1000 calls/day) ────────────
# Sign up: https://openweathermap.org/api
OPENWEATHERMAP_API_KEY=

# ── Ticketmaster Events (free tier) ───────────────────────
# Sign up: https://developer.ticketmaster.com/
TICKETMASTER_API_KEY=
